# API URL (set automatically during deployment)
NEXT_PUBLIC_API_URL=http://localhost:3001

# App URL for OGP image generation (set to your deployment URL in production)
NEXT_PUBLIC_APP_URL=http://localhost:3000

#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running type check..."
if ! pnpm type-check; then
  echo "❌ Type check failed"
  exit 1
fi

echo "✅ Type check passed"
echo ""

echo "🔎 Running lint..."
if ! pnpm lint; then
  echo "❌ Lint failed"
  exit 1
fi

echo "✅ Lint passed"
echo ""

echo "🛡️  Running CDK security check (cdk-nag)..."
if ! pnpm --filter @vote-board-game/infra synth > /dev/null 2>&1; then
  echo "❌ CDK security check failed"
  echo ""
  echo "Running synth again to show errors:"
  pnpm --filter @vote-board-game/infra synth
  exit 1
fi

echo "✅ CDK security check passed"
echo ""

echo "🧪 Running tests..."
if ! pnpm test; then
  echo "❌ Tests failed"
  exit 1
fi

echo "✅ Tests passed"
echo ""

echo "✨ All checks passed! Pushing to remote..."


